---
title: "Lab Log — Session N"
date: 2025-MM-DD
course: "Embedded Systems I (LIIE1214)"
student: "Name, ID"
group: "A / B"
commit: "abc1234"          # git commit hash of the code you're documenting
board: "Pico 2 (RP2350) SN/label: ____"
status: "draft"            # draft | complete
---

# Session N — Short Title

**Goal (one sentence):** ______

**Prediction — written *before* running anything:**
Expected value(s) and why (show the arithmetic): e.g. "TOP=999, div=150 → f = 150 MHz/(150·1000) = 1.000 kHz".

---

## Setup

- Pin map: GP__ → ______, GP__ → ______
- Photo of your breadboard with today's date on a card in frame: `img/sN_setup.jpg`
- Anything non-default (clock, pulls, external parts): ______

## What I did

Max 5 numbered steps. Commands or clicks only, no explanations.

1. ______
2. ______
3. ______

## Evidence

Every claim below must point to a capture. Scope/LA screenshots must show timestamp, cursors and your setup label; annotate them by hand (arrow + note).

| # | File | What it shows | Cursor/measured value |
|---|------|---------------|-----------------------|
| E1 | `img/sN_scope1.png` | ______ | ______ |
| E2 | | | |

## Predicted vs measured

| Quantity | Predicted | Measured (evidence #) | Error % | Why the difference? |
|----------|-----------|-----------------------|---------|---------------------|
| ______ | | E1 | | |

## What went wrong

At least one concrete thing (compile error, wrong pin, inverted logic, noisy signal…), what you saw, and how you found the fix. "Nothing went wrong" is not accepted.

- ______

## Code

Only the lines that matter (≤ 20). Everything else lives at commit `abc1234`.

```c
// e.g. the exact register write / SDK call that fixed the bug in "What went wrong"
```

## Open question

One thing you still don't understand or want to test next session.

- ______

## Repo  Structure

```
lablog-<id>/
├── mkdocs.yml                  # copy of your config, site_name = student
├── docs/
│   ├── index.md                # name, group, board label, link to course site
│   ├── sessions/
│   │   ├── 2025-08-20-s02.md   # one T0-style entry per session
│   │   ├── 2025-08-25-s03.md
│   │   └── ...
│   └── images/
│       └── s02_setup.jpg, s02_scope1.png ...
├── src/
│   ├── s02_blink_sio/          # one Pico project per session
│   │   ├── CMakeLists.txt
│   │   └── blink_sio.c
│   └── s03_fsm_debounce/
└── .github/workflows/ci.yml    # same Pages workflow → their own site
```